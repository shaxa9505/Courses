<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Markaz</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link <% if(active == 'home') {%> active text-primary <%}  %> " href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <% if(active == 'about') {%> active text-primary <%}  %> " href="/about">About</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <% if(active == 'courses') {%> active text-primary <%}  %> " href="/courses">Courses</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <% if(active == 'addCourse') {%> active text-primary <%}  %> " href="/addCourse">Addcourse</a>
                    </li>
            </ul>         
            <form class="d-flex" role="search">
                <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">Search</button>
            </form>
        </div>
    </div>
</nav>